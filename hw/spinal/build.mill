//| mill-version: 1.1.2
package build

import mill._
import mill.scalalib._

object spinal extends SbtModule {
  def scalaVersion = "2.13.12"
  override def moduleDir = super.moduleDir / os.up
  override def mvnDeps = Seq(mvn"com.github.spinalhdl::spinalhdl-core:1.12.0")
  override def scalacPluginMvnDeps = Seq(mvn"com.github.spinalhdl::spinalhdl-idsl-plugin:1.12.0")
}
